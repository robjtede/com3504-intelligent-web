extends layout

block main

  div.tweets
    if tweets
      p= 'Number of tweets: ' + tweets.length
      for tweet in tweets
        div.tweet
          p= tweet.datetime
          p= tweet.author
          p= tweet.content
